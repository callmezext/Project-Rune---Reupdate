/**
 * ═══════════════════════════════════════════════════════════
 *  RuneClipy — Discord Bot Manager
 *  This file is kept as a reference but the bot now runs
 *  as a standalone service in /bot/bot.js
 *  Control is done via MongoDB BotStatus collection.
 * ═══════════════════════════════════════════════════════════
 */

// The standalone bot (/bot/bot.js) handles all Discord bot logic.
// The admin dashboard sends commands via the BotStatus MongoDB collection.
// See: src/models/BotStatus.ts and src/app/api/admin/discord-bot/route.ts
export {};
